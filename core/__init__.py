"""OmniView core: ontology, bus, registry, schema.

The core owns exactly four things (see docs/core-constitution.md, local doc):
the ontology, the event bus, the registry + module contract, and the SDK door.
Domain knowledge is refused here by law - if core code names a domain, it is
misplaced.
"""
